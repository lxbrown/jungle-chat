{
  "name": "jungle-chat",
  "version": "0.1.0",
  "engines": {
    "node": "10.15.3"
  },
  "scripts": {
    "start-client": "cd client && yarn start",
    "start-server": "cd server && yarn start",
    "build": "(cd client && yarn build) && (cd ../server && yarn install)",
    "start": "yarn start-server"
  }
}
