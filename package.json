{
  "name": "jungle-chat",
  "version": "0.1.0",
  "engines": {
    "node": "10.15.3"
  },
  "scripts": {
    "build": "(cd client && npm install --only=dev && npm install && npm run build) && (cd server && npm install)",
    "start": "cd server && npm run start"
  }
}
